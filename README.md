# Koa Google Authentication

An example project template using Next.js as frontend and koa as backend.

Authentication with google oauth and then jwt tokens.

## .env file

```
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
DB_URL=
```
